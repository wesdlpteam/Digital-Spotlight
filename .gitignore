# Earlier/local-only version — not published
digital-space-spotlight.html

# Graphik brand fonts — licensed desktop cuts, kept OUT of this public repo.
# The @font-face rules reference fonts/*.woff2; if absent the UI falls back to
# Segoe UI / system sans (export stays Arial). Drop the licensed webfont files in
# locally, or serve them from a licensed host, before relying on Graphik publicly.
fonts/

# Brainstorming visual-companion scratch (mockups, server state)
.superpowers/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini

# link-helper downloaded binaries (fetched by Install-Helper.cmd)
link-helper/yt-dlp.exe
link-helper/gallery-dl.exe
link-helper/ffmpeg.exe
